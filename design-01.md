# Architecture & UI Design

## 1. Architectural Design

### 1.1 系统架构图

![architecture](img\architecture.png)

### 1.2 架构说明

本项目采用 **本地 Agent Runtime 为核心的分层单体架构**。系统没有拆分为多个独立微服务，而是将对话运行、任务调度、日历管理、技能管理、工具调用和本地持久化整合在同一个运行时中。这样可以降低部署和调试成本，也更适合桌面端或本地 sidecar 的使用场景。

该架构的核心目标是让 OpenCrab 不只是一个聊天界面，而是一个可扩展的本地智能代理系统。用户请求进入 Agent Runtime 后，由对话循环决定是否调用模型、工具、任务、日历、知识库或第三方服务。所有关键状态都会被持久化，因此系统支持历史恢复、任务追踪、文件复用和对话 checkpoint 恢复。

我们使用这种架构主要有以下原因：

1. **本地优先**：项目需要在桌面端稳定运行，使用本地数据库、文件系统和本地运行时可以减少对远程服务的依赖。
2. **功能复用**：任务、日历、技能、知识库等能力既可以被前端直接调用，也可以被 Agent 通过工具调用复用。
3. **可恢复性**：Agent 对话不是一次性请求，而是可能包含工具调用、中断、人类确认和继续执行，因此需要 checkpoint 和本地状态管理。
4. **可扩展性**：新工具、新技能、新第三方服务可以通过 Tools 或 Skills Hub 接入，而不需要重构核心对话流程。
5. **部署简单**：相比微服务架构，单一后端运行时更容易与 Tauri 桌面客户端打包和启动。

### 1.3 图中未完全体现的假设与约束

架构图没有完全展示以下实现假设：

1. 系统默认只有一个本地后端进程在运行，多个模块通过同一个 Agent Runtime 共享状态。
2. Agent 工具调用可能需要用户确认或 AI 审查，因此前端必须支持中断、审批和恢复执行的交互。
3. 文件解析、RAG 同步、外部课程系统等能力依赖外部服务质量，系统会允许局部失败而不是整体崩溃。
4. 由于项目面向桌面端，本地配置、凭据和通知能力是架构的重要组成部分。

------

## 2. UI Design

以下 UI 展示仅为设计稿，最终实现可能存在修改。

### 2.1 聊天主界面设计

![UI-01](img\UI-01.png)

聊天界面是系统的主要入口。设计目标是让用户可以自然地与 Agent 对话，同时清楚看到模型回复、工具调用、思考内容、附件和人工确认卡片。页面底部保留固定输入区，支持文本输入、文件附件、语音输入和停止生成。

### 2.2 任务管理界面设计

![UI-02](img\UI-02.png)

任务界面用于管理自动化任务和定时任务。设计上采用左侧任务列表、右侧任务详情的结构，方便用户快速切换任务、查看运行状态、编辑脚本、触发执行以及查看历史运行日志。环境变量管理作为同一功能域下的辅助页面。

### 2.3 日历界面设计

![UI-03](img\UI-03.png)

日历界面用于展示课程、日程和用户自定义事件。设计包含月份视图、事件列表、来源筛选和事件详情区域。用户可以按日期查看事件，也可以创建、编辑、删除日程，并通过不同颜色区分事件来源。

### 2.4 技能市场界面设计

![UI-04](img\UI-04.png)

技能市场用于浏览、搜索、下载、卸载和上传 OpenCrab Skills。设计上采用卡片式列表展示技能摘要，并提供标签筛选、搜索、详情弹窗和“我的投稿”入口，使用户能快速管理可扩展能力。

### 2.5 设置界面设计

![UI-05](img\UI-05.png)

设置界面用于管理系统运行所需的关键配置，包括模型接口、文件解析服务、OneBot、校园凭据、外观和通知偏好。设计采用左侧分类导航和右侧表单内容，减少配置项较多时的认知负担。